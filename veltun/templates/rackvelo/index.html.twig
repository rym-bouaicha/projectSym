{% extends 'base.html.twig' %}

{% block title %}Rackvelo index{% endblock %}

{% block body %}
    <h1>Rackvelo index</h1>

    <table class="table">
        <thead>
            <tr>
                <th>RefRack</th>
                <th>Capacite</th>
                <th>Modele</th>
                <th>actions</th>
            </tr>
        </thead>
        <tbody>
        {% for rackvelo in rackvelos %}
            <tr>
                <td>#{{ rackvelo.refRack }}</td>
                <td>{{ rackvelo.capacite }}</td>
                <td>{{ rackvelo.modele }}</td>
                <td>
                    <a href="{{ path('app_rackvelo_show', {'refRack': rackvelo.refRack}) }}">show</a>
                    <a href="{{ path('app_rackvelo_edit', {'refRack': rackvelo.refRack}) }}">edit</a>
                </td>
            </tr>
        {% else %}
            <tr>
                <td colspan="4">no records found</td>
            </tr>
        {% endfor %}
        </tbody>
    </table>

    <a href="{{ path('app_rackvelo_new') }}"><buttonc style=" padding: 1em 2em;"> Create new</buttonc></a>
{% endblock %}
